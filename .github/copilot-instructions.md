# Copilot Instructions

## Project Guidelines
- In Mermaid diagrams (e.g., in Markdown READMEs), avoid using `#` in participant labels/aliases or message text — Mermaid treats `#` as a comment marker and it breaks rendering. Use a substitute like "CSharp" instead of "C#".